<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="525">
    <Window.DataContext>
        <local:WindowVM/>
    </Window.DataContext>
    <Window.InputBindings>
        <KeyBinding Key="K" Command="{Binding moveLeft}"/>
        <KeyBinding Key="Space" Command="{Binding jump}"/>
    </Window.InputBindings>
    <Canvas>
        <ItemsControl ItemsSource="{Binding MajorToms}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Width="{Binding Size}" Source="Images1\Rock.png">
                        <Image.RenderTransform>
                            <TranslateTransform X="{Binding X}" Y="{Binding Y}"/>
                        </Image.RenderTransform>
                    </Image>

                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Canvas>
</Window>
